// Importing the images
import hello from "../assets/images/hello.png";
import glogo from "../assets/images/logo.png";
import landingImg from "../assets/images/landing-img.png";

// Creating an object to store the image names and their imports
const imagePaths = {
  hello,
  glogo,
  landingImg,
};

// Exporting the imagePaths object
export default imagePaths;
